generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}   

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String
}

model Products {
  id       String   @id @default(uuid())
  productName   String    
  productSize   String
}